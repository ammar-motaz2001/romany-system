// Application Configuration

// Backend disabled - Frontend only with mock data
export const USE_BACKEND = false;

// Backend API URL (not used in frontend-only mode)
export const BACKEND_URL = '';

// Enable offline mode automatically if backend fails
export const AUTO_FALLBACK_TO_MOCK = true;

// Show offline mode notification
export const SHOW_OFFLINE_NOTIFICATION = false;

// App version
export const APP_VERSION = '3.0.0';

// App name
export const APP_NAME = 'Hi Salon';

export default {
  USE_BACKEND,
  BACKEND_URL,
  AUTO_FALLBACK_TO_MOCK,
  SHOW_OFFLINE_NOTIFICATION,
  APP_VERSION,
  APP_NAME,
};